# sqlite_diffx
A developer-friendly, Git-like diff system for SQLite databases to Compare two SQLite databases and instantly see what changed — schemas, columns, data — with human-readable summaries, JSON output, and forward-only SQL migration patches.
